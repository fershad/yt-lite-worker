{
  "private": true,
  "name": "yt-lite-worker",
  "version": "1.0.0",
  "description": "A Cloudflare Worker that finds YouTube embeds on a page and replaces them with the Lite YouTube Embed facade.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "{{ authors }}",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.11",
    "himalaya": "^1.1.0"
  }
}
